package com.rottenapple.client;

/**
 * Legacy Weave entry point (v1). No longer used.
 *
 * <p>v2 injects via {@code com.rottenapple.agent.RottenAppleAgent}, a pure-JDK
 * java agent with no Weave / Mixin / Minecraft compile dependencies. This stub
 * is kept so anything still referencing the old class fails loudly instead of
 * silently.</p>
 */
@Deprecated
public final class RottenAppleMod {
    private RottenAppleMod() {}
}
