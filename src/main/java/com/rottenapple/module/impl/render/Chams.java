package com.rottenapple.module.impl.render;

import com.rottenapple.module.Module;

/**
 * Dropped: Raven's Chams visuals (GL polygon offset around players, player
 * hiding) require Forge render hooks, unavailable to a dep-free polling
 * agent. Stub kept so references fail loudly instead of silently.
 */
@Deprecated
public final class Chams extends Module {
    public Chams() {
        super("Chams", Category.render, 0);
    }
}
