package com.rottenapple.module;

/** Section header grouping settings (mirrors Raven's GroupSetting). */
public class GroupSetting extends Setting {
    public GroupSetting(String name) {
        super(name);
    }
}
